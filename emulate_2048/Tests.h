#pragma once

/*
 * Unfortunatly, right now, I don't
 * know how to do unit tests 
 * properly, so I do my best

 * Fortunately, due to the nature 
 * of the task
 * it's really easy and fast
 * to test it manually
 
 * However, this is no reason
 * not to add more tests
 *
 * TODO: add more tests
 */
namespace Game_2048 {
    class Tests
    {
    struct BoardTests;
    public:
        static bool run();
    };
}

